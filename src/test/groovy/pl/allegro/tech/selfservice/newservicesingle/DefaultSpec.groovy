package pl.allegro.tech.selfservice.newservicesingle

import spock.lang.Specification

class DefaultSpec extends Specification {

    def "should contain at least one test case"() {
        when:
        def x = 1
        def y = 1

        then:
        x == y
    }
}
