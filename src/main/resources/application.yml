application:
  name: chi-server

discovery:
  enabled: false

metadata:
  cloud:
    skipReading: true

metrics:
  graphite:
    enabled: false

chi:
  experiments:
    file: "http://assets-chi.qxlint/chi-server/experiments-test.json"
  client:
    maxConnections: 200
    maxConnectionsPerRoute: 10
    connectionTimeout: 200
    socketTimeout: 300
    name: assets-chi

hermes:
  url: service://hermes
  client:
    maxConnections: 200
    maxConnectionsPerRoute: 10
    connectionTimeout: 200
    socketTimeout: 300
    name: hermes

analytics.hermes:
  topic: pl.allegro.experiments.chi.assignments
  schemaVersion: 1

avro:
  enabled: true
  serializer: jackson
  schema:
    applicationSchemaProvider: classpath
    mapping:
      - className: pl.allegro.experiments.chi.chiserver.analytics.ExperimentAssignment
        schemaVersion: 1
        schemaLocation: /avro/pl.allegro.experiment.chi.chiserver.analytics.ExperimentAssignment.avsc