discovery:
  enabled: true
  type: consul

metadata:
  cloud:
    skipReading: true

metrics:
  graphite:
    enabled: true
    host: skystats-test.qxlint

assignments:
  repository: kafka
  buffer-size: 10000
  flush-period: 500